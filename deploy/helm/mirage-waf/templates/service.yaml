apiVersion: v1
kind: Service
metadata:
  name: {{ include "mirage-waf.fullname" . }}
  labels:
    {{- include "mirage-waf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mirage-waf.selectorLabels" . | nindent 4 }}

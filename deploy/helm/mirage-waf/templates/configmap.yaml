apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mirage-waf.fullname" . }}-config
  labels:
    {{- include "mirage-waf.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

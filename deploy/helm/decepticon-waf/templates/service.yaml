apiVersion: v1
kind: Service
metadata:
  name: {{ include "decepticon-waf.fullname" . }}
  labels:
    {{- include "decepticon-waf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "decepticon-waf.selectorLabels" . | nindent 4 }}
